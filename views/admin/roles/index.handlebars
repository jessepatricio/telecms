<div class="row">
    <div class="col-sm-8">
        <h2>Role List</h2>
        <div style="margin-bottom: 15px;">
            <a href="/admin/roles/create" class="btn btn-success btn-sm">Create</a>
        </div>
        <table class="table">
            <thead>
                <tr>
                    <th>Role</th>
                    <th>Date</th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                {{#each roles}}
                <tr>
                    <td>{{rolename}}</td>
                    <td>{{formatDate date 'DD/MM/YYYY'}}</td>
                    <td>
                        <a href="/admin/roles/edit/{{id}}" class="btn btn-info btn-sm">Edit</a>
                    </td>
                    <td>
                        <form action="/admin/roles/{{id}}?_method=DELETE" method="post">
                            <button class="btn btn-danger btn-sm">Delete</button>
                        </form>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
</div>