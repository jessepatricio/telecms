<div class="row">
    <div class="col-sm-8">
        <h2>Cabinets</h2>
        <div style="margin-bottom: 15px;">
            <a href="/admin/cabinets/create" class="btn btn-success btn-sm">Create</a>
        </div>
        <table class="table table-responsive table-hover">
            <thead>
                <tr>
                    <th>Cabinet</th>
                    <th>Laterals</th>
                    <th>Date</th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                {{#each cabinets}}
                <tr>
                    <td>{{name}}</td>
                    <td>{{dropcount}}</td>
                    <td>{{formatDate date 'DD/MM/YYYY'}}</td>
                    <td>
                        <a href="/admin/cabinets/edit/{{id}}" class="btn btn-info btn-sm">Edit</a>
                    </td>
                    <td>
                        <form action="/admin/cabinets/{{id}}?_method=DELETE" method="post">
                            <button class="btn btn-danger btn-sm">Delete</button>
                        </form>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
</div>